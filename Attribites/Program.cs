using Characteristics;
using Characteristics.TypeModificator;
using Characteristics.Parameter;
using System.Collections.Generic;

class Program
{
    static void Main(string[] args)
    {
        //Dictionary<string, UnitParameter> parameters = new Dictionary<string, UnitParameter>();
        //Health health = new Health(100);
        //Armor armor = new Armor();
        
        //parameters.Add(health.Name, health);
        //parameters.Add(armor.Name, armor);
        //List<Modificator<double>> effects = new List<Modificator<double>>();
        //effects.Add(new InValue("Test", "HP", 120, 10));
        //effects.Add(new InPercent("Test", "HP", 120, 2));

        //effects.RemoveAll(e => e.LeftTicks < 0); //удаляем просроченные эффекты
        //foreach (var effect in effects)
        //{
        //    UnitParameter param;
        //    if (parameters.TryGetValue(effect.ParamName, out param))
        //    {
        //        if(param is not null) param.CurrentMods.Add(effect);
        //    }
        //    effect.Update();
        //}       
    }
}